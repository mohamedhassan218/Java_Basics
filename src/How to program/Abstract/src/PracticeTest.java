import java.util.Scanner;
public class PracticeTest 
{
	public static void main(String[] args) 
   {
		
		//Q1
		//Problem for group 2: 
		/*
		 * int x; int k; double total = 0;
		 * 
		 * Scanner input = new Scanner(System.in);
		 * 
		 * 
		 * System.out.print("Enter X: "); x = input.nextInt(); System.out.println();
		 * 
		 * System.out.print("Enter K: "); k = input.nextInt(); System.out.println();
		 * 
		 * for(int i = 1; i <= k; i++) { if (i%2 == 0) { total -= (Math.pow(x, i) / i);
		 * } else if (i%2 == 1) { total += (Math.pow(x, i) / i); } }
		 * 
		 * total = Math.abs(total); System.out.println(total);
		 */
		
		//******************************************************************************
		//Q2
		//Problem for group 1:
		/*
		 * int x ; int k; double total = 0;
		 * 
		 * Scanner input = new Scanner(System.in); System.out.print("Enter X: "); x =
		 * input.nextInt(); System.out.println();
		 * 
		 * System.out.print("Enter K: "); k = input.nextInt(); System.out.println();
		 * 
		 * int denominator = 1; for(int i = 1; i <= k; i++) { total += (Math.pow(x, i) /
		 * (denominator * i)); denominator *= i; }
		 * 
		 * System.out.println(total + 1 );
		 */
    
    }
}
